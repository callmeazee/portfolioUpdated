import type { TechGroup } from "@/types/content";

/**
 * Grouped, never rated. README §30 non-negotiable rule 15 and content.md §30
 * both forbid skill percentage bars — the grouping is the whole model.
 */
export const skills: TechGroup[] = [];
